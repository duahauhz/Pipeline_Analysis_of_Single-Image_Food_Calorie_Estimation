# Python deps for the YOLOv13 + ECUSTFD submission
# Tested with Python 3.11 + CUDA 12.1

# --- Core ML / CV stack (install torch separately, see README) ---
numpy==1.26.4
opencv-python==4.9.0.80
pillow>=10.0.0
matplotlib>=3.7.0
pandas>=1.5.0
scipy==1.13.0
scikit-learn>=1.3.0
seaborn>=0.12.0

# --- PyTorch ecosystem (install torch first via https://pytorch.org) ---
# torch==2.2.2
# torchvision==0.17.2

# --- YOLOv13 / Ultralytics ---
# Install via: pip install -e yolov13 --no-deps --no-build-isolation
ultralytics-thop>=2.0.0
psutil>=5.9.0
py-cpuinfo>=9.0.0
tqdm>=4.64.0
requests>=2.23.0
pyyaml>=5.3.1
timm==1.0.14
huggingface-hub==0.23.2
safetensors==0.4.3
supervision==0.22.0

# --- Dataset / annotations ---
pycocotools==2.0.7

# --- Density workbook parser (xlrd for .xls) ---
xlrd==2.0.1

# --- Optional: ONNX / TRT export (only needed if you run export_tensorrt.py) ---
# onnx==1.14.0
# onnxruntime==1.15.1
# onnxruntime-gpu==1.18.0
# onnxslim==0.1.31